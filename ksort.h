#ifndef KSORT_H
#define KSORT_H

#include <vector>

template <class T>
void ksort(T &t,size_t k){
    T ret;
    size_t s = t.size()/k;
    vector<T> rts();
    for(size_t i = 0;i != k;++ i){
        T tt()
        heapsort(tt, k);
    }
}



#endif // KSORT_H
